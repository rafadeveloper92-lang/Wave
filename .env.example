# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# SUPABASE_URL: The URL of your Supabase project.
VITE_SUPABASE_URL="https://hhgecmtljovgoqbcqleb.supabase.co"

# SUPABASE_ANON_KEY: The anonymous key for your Supabase project.
VITE_SUPABASE_ANON_KEY="sb_publishable_KR3obEpqz9WJZXWwUVS5xw_iq_rVUAy"

# Mensagens em tempo real: execute supabase/migrations/001_wave_messaging.sql no SQL Editor
# e em Database > Replication adicione a tabela public.chat_messages ao publication supabase_realtime.

# Push (Android com Capacitor): configure Firebase no projeto nativo; o app grava tokens em push_tokens.
# O envio de notificações exige uma Edge Function ou servidor com FCM server key.
